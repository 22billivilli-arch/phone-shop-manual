# /api/.htaccess — 설정·공통 PHP 파일 직접 접근 차단 (API 엔드포인트만 허용)
<FilesMatch "^(config|_common)\.php$">
  <IfModule mod_authz_core.c>
    Require all denied
  </IfModule>
  <IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
  </IfModule>
</FilesMatch>
